\name{import_Shimadzu_TOC}
\alias{import_Shimadzu_TOC}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
Import function for output data from Shimadzu TOC-L/TNM-L
}
\description{
This function imports output files generated by the Shimadzu TOC-L/TNM-L analyser equipped with an ASI-L autosampler. Columns are renamed by default. The file to be read in is expected to be in .txt format.
}
\usage{
import_Shimadzu_TOC(file, headerLang = c("eng", "ger"))
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{file}{
    The name of the file which the data are to be read from. Supported are export files in .txt format originated by the TOC-L software.
%%     ~~Describe \code{x} here~~
}
  \item{headerLang}{

  }
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
The returned dataframe holds the following columns:
  \item{type}{
Replaces "Typ".
  }
  \item{analysisMode}{
Replaces "Anal.".
  }
  \item{sampleName}{
Replaces "Probenname".
  }
  \item{sampleID}{
Replaces "ProbenID".
  }
  \item{method_Used}{
Replaces "Original".
  }
  \item{calCurve_Used}{
Replaces "Kal.Kurve".
  }
  \item{dillManual}{
Replaces "Manuelle Verduennung".
  }
  \item{note}{
Replaces "Anmerk.".
  }
  \item{comment}{
Replaces "Kommentar".
  }
  \item{dateTime}{
Replaces "Datum / Uhrzeit".
  }
  \item{sampleNo}{
Replaces "Prb. Nr.".
  }
  \item{injNo}{
Replaces "Inj. Nr.".
  }
  \item{injAnalyte}{
Replaces "Analyse (Inj.)".
  }
  \item{area}{
Replaces "Fläche".
  }
  \item{areaMean}{
Replaces "MW Fläche".
  }
  \item{conc}{
Replaces "Konz.".
  }
  \item{concMean}{
Replaces "MW Konz.".
  }
  \item{result}{
Replaces "Ergebnis".
  }
  \item{resultMean}{
Replaces "MW Ergebnis".
  }
  \item{remark}{
Replaces "Bemerk.".
  }
  \item{areaSD}{
Replaces "STD. Abw. Fläche".
  }
  \item{areaCV}{
Replaces "VK Fläche".
  }
  \item{concSD}{
Replaces "Std. Abw. Konz".
  }
  \item{concCV}{
Replaces "VK Konc".
  }
  \item{samplePosition}{
Replaces "Probenpos.".
  }
  \item{excluded}{
Replaces "Ausgeschl.".
  }
  \item{injVol}{
Replaces "Inj. Vol.".
  }
  \item{dillAuto}{
Replaces "Auto. Verd.".
  }
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
Anıl A. Tellbüscher
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
function (x)
{
  }
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory (show via RShowDoc("KEYWORDS")):
% \keyword{ ~kwd1 }
% \keyword{ ~kwd2 }
% Use only one keyword per line.
% For non-standard keywords, use \concept instead of \keyword:
% \concept{ ~cpt1 }
% \concept{ ~cpt2 }
% Use only one concept per line.
